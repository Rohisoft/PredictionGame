# Base URL of the Node.js API in server/ — include the /api prefix.
# Local dev (server/ running via `npm run dev`):
VITE_API_URL=http://localhost:4000/api

# Production: your deployed API's URL, e.g. https://api.yourapp.com/api
